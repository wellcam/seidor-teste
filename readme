Agradeço pela oportunidade de participar desse processo.

Node.js não é minha linguagem de domínio mas foi muito prazeroso realizar esse teste. Com certeza há muito o que melhorar e fiz o melhor que eu podia dentro do tempo estabelecido.
Obrigado.


=== PRÉ REQUISITOS ===
Utilizado:
	Npm
		npm install -g npm
	Node.js
		sudo apt-get install nodejs
	Express
		npm install express
	Sequelize
		npm install sequelize sequelize-cli path
	Momemt
		npm install moment
	Body-Parser
		npm install body-parser
	MySQL
		npm install mysql
	Nodemon - não necessário instalar


=== INICIANDO PROJETO ===
Para que o projeto tenha seu inicio é necessário ter os itens listados acima instalados com o npm.

Após a instalação. Execute os comandos no terminal do VSCode:
	1. npx sequelize-cli db:migrate
	2. npx sequelize-cli db:seed:all


Assim a nossos model serão criados como tabelas no banco de dados e também populado.

=== POSTMAN ===
Na estrutura do projeto possui uma pasta 'postman' e possui a collection das APIs solicitadas no teste.